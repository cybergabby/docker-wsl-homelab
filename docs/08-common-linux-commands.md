# Common Linux Commands Used

## Navigation

```bash
pwd
ls
cd
mkdir
rm
cp
mv
```

---

## User Information

```bash
whoami
groups
id
```

---

## Package Management

```bash
sudo apt update
sudo apt install
sudo apt remove
sudo apt autoremove
```

---

## File Permissions

```bash
chmod
chown
ls -l
```

---

## Docker

```bash
docker images
docker ps
docker build
docker run
docker logs
docker stop
docker rm
```

These commands formed the foundation of this homelab.
